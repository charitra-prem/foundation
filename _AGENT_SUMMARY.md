The `/about` route is implemented at `src/app/about.tsx`, rendering the app name and version from `app.json` via `expo-constants` (`Constants.expoConfig.name` and `Constants.expoConfig.version`). The existing test in `src/app/__tests__/screens.test.tsx` covers the AboutScreen with a mocked `expo-constants` value. All five screen tests pass with `pnpm test`, and `npx tsc --noEmit` type-checks cleanly, so the acceptance criteria for adding a `/about` screen are satisfied.
