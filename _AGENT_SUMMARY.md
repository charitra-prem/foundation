Implemented the expo-router foundation scaffold (AIO-14). Updated `package.json` to use `expo-router/entry` as the entry point with Expo SDK 56 dependencies (expo ~56.0.12, expo-router ~56.2.11, react 19.2.3, react-native 0.85.3, react-native-screens, react-native-safe-area-context, jest-expo for tests). Updated `app.json` with Expo config including `scheme: "foundation"`, Metro web bundler, `newArchEnabled: true`, and `typedRoutes` experiment. Added `babel.config.js` (babel-preset-expo), `tsconfig.json` (strict mode, `@/*` path alias), and `expo-env.d.ts` for typed routes. Created the file-based routing tree under `src/app/`: root `_layout.tsx` (Stack navigator with splash-screen hide), `(auth)/` group with `_layout.tsx`, `sign-in.tsx`, and `sign-up.tsx` placeholder screens (ready for Clerk integration in a later issue), `(tabs)/` group with `_layout.tsx` (bottom tab navigator), `index.tsx` (Home), and `settings.tsx` (Settings), plus a `+not-found.tsx` 404 screen. Added `src/app/__tests__/screens.test.tsx` with four smoke tests verifying each base screen renders without errors, using `jest.mock('expo-router')` to isolate the Link component.
