Added a new `/about` route at `src/app/about.tsx` that displays the app name and version sourced from `app.json` via `expo-constants` (`Constants.expoConfig.name` and `Constants.expoConfig.version`), following the existing screen style. Updated `src/app/__tests__/screens.test.tsx` with a new smoke test for `AboutScreen` and a mock for `expo-constants`. Also fixed the test harness by adding missing dev dependencies `jest` and `@react-native/jest-preset` to `package.json`, and verified all five screen tests pass with `npm test`.