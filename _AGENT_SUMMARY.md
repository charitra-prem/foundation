The `/about` screen is implemented at `src/app/about.tsx`; it reads `name` and `version` from `app.json` via `expo-constants` (`Constants.expoConfig`) and renders them in a centered view. The existing test in `src/app/__tests__/screens.test.tsx` covers the AboutScreen, including its mocked app name and version, and all five screen tests pass with `pnpm test`. The only change required was regenerating `pnpm-lock.yaml` with `pnpm install --no-frozen-lockfile` so it matches the `@types/jest` specifier already present in `package.json`, resolving the prior frozen-lockfile install failure. TypeScript also type-checks cleanly with `npx tsc --noEmit`.
