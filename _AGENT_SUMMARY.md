Verified the existing `/about` route at `src/app/about.tsx` correctly displays the app name and version from `app.json` via `expo-constants` (`Constants.expoConfig.name` and `Constants.expoConfig.version`). The existing test in `src/app/__tests__/screens.test.tsx` already covers the AboutScreen render. Added the missing `@types/jest` dev dependency to `package.json` so the Jest globals used in the test file type-check cleanly with `npx tsc --noEmit`. Confirmed all five screen tests pass with `npm test`.
